# Set One

def hex_to_base64(hexadecimal):
    """
    Convert hexadecimal to base64
    """

    return hexadecimal

def fixed_xor(buffer_one, buffer_two):
    """
    XOR two buffers
    """
    return buffer_one + buffer_two

if __name__ == "__main__":
    self.main()
